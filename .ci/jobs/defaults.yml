---

##### GLOBAL METADATA

- meta:
    cluster: apm-ci

##### JOB DEFAULTS

- job:
    logrotate:
      numToKeep: 300
    publishers:
    - email:
        recipients: infra-root+build@elastic.co
    prune-dead-branches: true
